import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "media.sirelsworth.odinsglasses",
  appName: "Odin’s Glasses",
  webDir: "renderer-dist",
  android: {
    backgroundColor: "#0f172a",
  },
};

export default config;
